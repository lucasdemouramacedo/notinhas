import { createStore } from 'vuex'

export default createStore({
  state: {//onde vão estar os dados iniciais da aplicação tipo o data
    primaryColor: '#20BF6B',
  },
  getters: {
  },
  mutations: { //onde se muda o estado
  },
  actions: {//são ações dentro de um contexto
  },
  modules: {
  }
})
